'use client'

import { observer } from 'mobx-react-lite'

const Home = observer(() => {
  // const auth = useAuth()
  // const router = useRouter()
  // useEffect(() => {
  //   if (!auth.isAuth) {
  //     router.replace('/profile')
  //   }
  // }, [auth.isAuth, router])

  return null
})
export default Home
